# AI Agent 运行规则

本 Vault 是人类与 Codex、Claude Code、Cursor 等 AI Agent 共同使用的长期知识系统。

## 开始前必读

执行重要工作前阅读：

1. `98-AI-上下文/AI运行上下文.md`
2. `98-AI-上下文/当前重点.md`
3. `98-AI-上下文/提示词规则.md`
4. 相关项目的 `项目状态.md`

## 信息架构

- 每篇笔记只能有一个主目录分类。
- 一篇笔记可以有多个 `topic/...` 标签和内部链接。
- 原始收集资料保存在 `00-收件箱/已下载`，绝不能覆盖。
- 清洗副本进入 `00-收件箱/已清洗`；正式研究资料归入 `04-研究` 下的一个目录。
- 主题中心保存在 `04-研究/主题中心`。
- 可复用提示词只能放在 `89-提示词` 下的一个分类中。
- 已完成或停用的资料移入 `99-归档`；未经明确批准，不删除长期知识。

## 长期记忆

不要保存聊天记录。只用可能长期有用的信息更新记忆：

- 决策及理由；
- 已验证的工作流和最佳实践；
- 重复出现的经验和用户偏好；
- 项目状态、风险和下一步；
- 稳定的知识摘要或重要趋势变化。

将这些更新写入 `97-AI-记忆`、`98-AI-上下文` 或相关的 `06-项目/<项目>/项目状态.md`。

## 研究与来源

- 保留来源原意，区分来源事实和分析。
- 尽可能保留 `source_url`、作者、发布日期和收集日期。
- 研究资料清洗器可以改善结构，但不能删除、总结或改写来源观点。
- 添加相关 `topic/...` 标签和主题中心链接。
- 优先添加有助于检索的内部链接，避免装饰性链接堆积。

## 项目

每个项目目录都必须包含 `项目状态.md`，记录：

- 当前状态；
- 已完成工作；
- 待办事项；
- 下一步行动；
- 风险；
- 决策。

取得实质进展后更新。

## 验证

- 提交前审查变更文件。
- 结构调整后运行 `98-AI-上下文/自动化/知识库巡检.ps1`。
- 不提交秘密信息、令牌、本地缓存或机器专属工作区状态。
